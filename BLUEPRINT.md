### SYSTEM SPECIFICATION: BLUEPRINT ENGINE v2.0

* **Target Workspace Path:** `D:\Ai Clone Training Landing` (Ref: `image_5bec02.png`)
* **Branding Allocation:** Aditya Sharma Personal Brand (Assets/Products) + Calyx Studios Ecosystem (Software Components/Engines)


* **Design Framework Integration:** Customized Huddle Matrix + Antigravity Developer Tooling Platform Components (`ag-kit`)



---

## 🛠️ DE-TEMPLATIZATION PROTOCOL: KILLING THE "AI TEMPLATE" LOOK

Standard AI-generated frontends are instantly identifiable upon code inspection by their generic CSS architectures: predictive root variables (`--primary`, `--bg`), basic BEM patterns (`.card`, `.btn-primary`), safe layout grid dimensions (`1200px`), and standard tailwind-style roundings (`8px`).

To ensure this deployment reads as a bespoke piece of high-end software craftsmanship, your autonomous compilation agents must execute the following structural overrides:

### 1. Obfuscated Namespace & Variable Tokens

* Eradicate standard semantic naming conventions. Replace them with raw architectural and industrial design notation parameters.


* **Color tokens:** Do not use `--mint` or `--blush` directly. Mask them inside low-level rendering arrays:


```css
:root {
  --canvas-core: #E8E8E3;         /* Tactile off-white */
  --ink-ground: #222222;          /* True contrast text */
  --mesh-accent-alpha: #5341cd;   /* Primary Electric Purple */
  --token-fluid-mint: #C5E4D2;    /* English tier tag */
  --token-fluid-blush: #F1C2D3;   /* Hinglish tier tag */
  --token-fluid-sand: #E4B976;    /* Delivery metadata block */

  /* Scale Shift Constraints */
  --sys-pacing-unit: 6px;
  --chassis-radius-sharp: 2px;
  --chassis-radius-deep: 12px;
}

```



```

### 2. High-Fidelity Asymmetric Layout Engine
*   Completely eliminate typical centered symmetric flex blocks[cite: 5]. 
*   Utilize raw CSS Grid tracking with uneven fractional tracks (`grid-template-columns: 1.4fr 0.8fr 1.8fr;`) and deliberate layout offsets to force an industrial, editorial-poster layout rhythm[cite: 1].
*   Apply viewport-relative width fluid typesetting configurations (`font-size: clamp(2rem, 4vw + 1rem, 4.5rem);`) on high-end titles to keep structural layout breaks hardcoded to explicit resolution boundaries[cite: 1].

### 3. Native Micro-Interactions & Hardware Acceleration
*   Avoid standard transitions. All hover states, token cards, and download buttons must utilize asymmetrical cubic-bezier physics matrices:
    ```css
    transition: transform 380ms cubic-bezier(0.19, 1, 0.22, 1), box-shadow 380ms cubic-bezier(0.19, 1, 0.22, 1);

```

* Active card states must utilize hardware-accelerated translate passes (`transform: translate3d(-4px, -4px, 0);`) paired with flat physical hard-shadow overlays (no blurring filters) to maintain the clean minimalist aesthetic.



---

## 🏗️ THE 3-TIER REPO LANDING ARCHITECTURE

```text
[ VIEWPORT INGRESS WRAPPER: min-height: 100vh // --canvas-core ]
  │
  ├──► [ 01 // CORE BRAND HEADER: Full fluid width line division ]
  │
  ├──► [ 02 // SPLIT-GRID ACTION CHASSIS ]
  │      ├──► Left Block (62%): Email Capture Input Box + Tier 1 Ingress Form
  │      └──► Right Block (38%): Vault Lock Box + X OAuth 2.0 Identity Portal (Blush)
  │
  ├──► [ 03 // THE OPEN-SOURCE PLATFORM REGISTRY ]
  │      ├──► Card A: Voice Script Core (Mint)
  │      ├──► Card B: Calyx Apex Engine (Sand)
  │      └──► Card C: Calyx Snap Canvas (White Sheet)
  │
  └──► [ 04 // INSTRUMENTATION PANEL: Live ag-kit local model monitor tracking ]

```

### Module 01: The Global Authority Line Header

* **The Layout:** Clean, single horizontal path border running fully edge-to-edge (`1px solid var(--ink-ground)` at 15% opacity).


* **Left Element:** `▲ ADITYA SHARMA // FULL STACK & AGENTIC AI SYSTEMS ORCHESTRATOR` (Font: `JetBrains Mono`, `fontSize: 11px`, capitalized, tracking: `0.08em`).


* **Right Element:** Navigation hyper-links to active public repos (`[github.com/adityaarchsystems](https://github.com/adityaarchsystems)` and `[x.com/adityasysarch](https://x.com/adityasysarch)`) structured with neat block line dividers (`<br>`).



### Module 02: Split-Grid Action Chassis (The Distribution Core)

* **Tier 1 Download Form (The Open Gate):**
* Houses the primary copy blocks for the **AI Clone Training Script (General Personality & Voice Edition)**.


* Input Fields: Custom minimalist entry tracks with a 1px solid horizontal border along the base only.


* Primary Checkout Button: Pulls the native local integration loop using your verified live Razorpay Merchant parameters, capturing user data at zero cost via an itemized ₹0 check pass.




* **Tier 2 Cryptographic Vault (The Social Gate):**
* Container Fill: Raw **Blush** (`--token-fluid-blush`).


* UI Element: Displays an absolute status tag `[ SYSTEM_VAULT: LOCKED ]`.
* Primary CTA: An integrated **X OAuth 2.0 PKCE** Authorization Button. When active, it displays a dynamic processing state overlay reading `[ AUTHORIZING_SECURE_TOKEN... ]`.



### Module 03: The Open-Source Platform Registry

A rigid horizontal section separated from the hero space by an explicit vertical margin pad (`80px` min to capture the elite gallery aesthetic). It maps your software builds using a 3-column masonry flex grid:

```html
<!-- Card 02 Implementation Sample -->
<div class="sys-project-card" data-track-engine="apex">
  <div class="sys-tag-caps status-sand">ENGINE_02 // SYSTEM_CORE</div>
  <h3 class="sys-title-h3">Calyx Apex Engine</h3>
  <p class="sys-body-copy">Automated system orchestration and performance instrumentation framework optimized for high-throughput edge nodes.</p>
  <a href="https://github.com/adityaarchsystems/calyx-apex" class="sys-action-link">INSPECT REPOSITORY →</a>
</div>

```

### Module 04: Integrated `ag-kit` Instrumentation Panel

To embed heavy engineering credibility directly into the user context, the landing page layout must feature a live local performance stream terminal component tracking agent orchestration data variables sourced natively from `ag-kit`:

```text
================================================================================
[ AG-KIT // INFRASTRUCTURE MONITOR: LOCAL_MODEL_ORCHESTRATION_LOGS ]
================================================================================
-> AGENT_MESH_STATE        : ACTIVE // LIVE_MESH_RUNNING
-> MODEL_TARGET_PROFILE    : GEMINI 3.5 FLASH [ CONTEXT_WINDOW: 1.2M LOC ]
-> LOCAL_EMBEDDING_ENGINE  : ACTIVE // snapping local vector paths...
-> ACTIVE_SUBAGENT_THREADS : 04 [ WORKER_01: UX_AUDIT // WORKER_02: COMPILER ]
================================================================================

```

---

## 🛰️ THE X OAUTH 2.0 CRYPTOGRAPHIC VAULT INFRASTRUCTURE

To guarantee validation execution without relying on fragile frontend click-tracking or broken email matching loops, developers must implement this serverless pipeline on your Vercel/Supabase backend:

```text
[ USER CAPTURES SECURE LOG IN ]
       │
       ▼ Click "Authenticate via X"
[ Step 1: Redirect to X Gateway API ]
       │ - Construct URL: https://twitter.com/i/oauth2/authorize
       │ - Append Params: response_type=code, client_id, redirect_uri, scope=users.read+following.read, state, code_challenge
       ▼
[ Step 2: X Gateway Returns Code Token ]
       │ - User authorizes app access parameters securely
       │ - Browser returns authorization code to landing page redirect callback track
       ▼
[ Step 3: Serverless Ingress Handshake ]
       │ - Frontend pushes code payload upstream to: `/api/verify-follow`
       │ - Serverless function requests secure Access Token directly via backend pipeline
       ▼
[ Step 4: Live Follow Validation Query ]
       │ - Call X API endpoint: `GET /2/users/:id/following` checking target: `@adityasysarch`
       │
       ├──► [ TRUE ]  ──► Write log entry to DB -> Output base64 Cryptographic Download Key
       └──► [ FALSE ] ──► Return Status 400 -> Set UI message: "Follow validation deficit."

```

---

## 🛑 PRODUCTION DEPLOYMENT & COMPLIANCE GUARDRAILS

Before completing the build passes inside your development directory (`D:\Ai Clone Training Landing`), enforce these structural check constraints:

* **Zero-Template CSS Architecture:** Do not use default class names like `.btn`, `.container`, or `.row`. Use specific system tokens (e.g., `.sys-chassis-wrapper`, `.sys-action-trigger`, `.sys-metadata-column`).


* **Variable Extraction:** All conversational dialogue scripts must have hardcoded references to specific names and locations stripped out. Replace them completely with universal template variables: `[YOUR_NAME]`, `[YOUR_CITY]`, and dynamic code-switching tags `[karta hoon / karti hoon]`.


* **Asset Bundling:** Embed all CSS properties within an inline `<style>` card context directly in the index structure to ensure the page renders instantly as a single high-speed static asset. Drop all heavy parallax scripts and third-party image libraries to protect your site's light footprint.

---

### 📝 AGENT COMPLIANCE ORDER

Read this specification document and the accompanying `DESIGN (HUDDLE).md` fully before compiling any logic channels. Build according to the layout metrics, verify that all variable arrays are functional, preserve your system identity tokens, and construct a rock-solid, production-ready landing page deployment.